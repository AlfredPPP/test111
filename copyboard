import re

line = "你的行数据在这里"  # 替换为你从PDF读取的实际行数据

# 正则表达式来匹配数字，包括可能的前导小数点
pattern = r'(?<!\S)\d*\.\d+|\d{1,3}(?:,\d{3})*(?:\.\d+)?(?!\S)'

# 使用findall方法提取所有匹配的数字
matches = re.findall(pattern, line)

print(matches)